__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/

# MLflow local tracking store & registry DB (regenerated by `python src/train.py`)
mlflow.db
mlruns/
mlartifacts/

# Model artifacts (regenerated by training; the model itself lives in the MLflow registry)
models/*.joblib

# DVC cache (managed by DVC, not Git)
.dvc/cache
.dvc/tmp

.DS_Store
